# macOS-specific tmux configuration

# No special settings needed for modern macOS
# - reattach-to-user-namespace is no longer required
# - Clipboard works via OSC 52 with set-clipboard on

# iTerm2 integration notes:
# - Use 'tmux -CC' for native iTerm2 integration
# - See README.md for details
