# 個人設定

## 基本姿勢

### トレードオフスライダー

スコープ: 大
予算: 小
納期: 最小
品質: 最大

### 改善の約束は行動で示す

「意識します」「気をつけます」「メモリに残します」のような宣言は禁止。これらは次のセッションや別の環境に引き継がれず、実効性がない。

改善が必要な場合は、必ず以下のいずれかの行動を取ること:

- **CLAUDE.md / AGENTS.md にルールを追記する**（再発防止策をプロンプトに残す）
- **GitHub Issue を作成する**（修正が必要な箇所を記録する）
- **その場で修正する**（コード・設定・ドキュメントを直接変更する）

行動を伴わない反省文は出力しない。

### スキル優先呼び出し

対応するスキルが存在する場合は必ずスキル経由で実行する。手動でスキルのロジックを再現しない。

## 対話ルール

### 判断要求の前置き

`AskUserQuestion` で重要な判断（方針変更、リスクある操作、スコープ修正等）を問う前は、
必ず直前に「## まとめ」または「## 現状」セクションを出力する。
含めるべき情報:
- 背景（なぜ判断が必要になったか）
- 各選択肢の trade-off（リスク・コスト・期待結果）
- 推奨案がある場合はその根拠

## コマンド実行ルール

### シェルコマンド置換 `$(...)` の絶対禁止

`$(...)` を含むコマンドは安全ヒューリスティクスにより毎回確認ダイアログが出る。
**コマンド生成時は `$(...)` を絶対に使わないこと。**

> **注意**: PreToolUse hook でのブロックは許可ダイアログより後に実行されるため効果がない。
> この制約は CLAUDE.md の指示のみで担保している。

- git commit は `-m "message"` または `-m "1行目" -m "2行目"` を使う
  - ヒアドキュメント形式 `$(cat <<'EOF'...)` も禁止
- 変数代入が必要な場合は複数ステップに分割する
- パイプ (`|`) やリダイレクト (`>`) は問題ない
- `$(...)` やバッククォート は全て確認ダイアログの対象になる

### commit メッセージ内のバッククォート禁止

commit メッセージ内のバッククォート（`` ` ``）は zsh OOM クラッシュを誘発するため絶対禁止。

- `-m "...` `` ` `` `code` `` ` `` `..."` のような直書きは bash がコマンド置換として展開し、zsh `command_not_found_handler` 無限再帰で fatal error: out of memory に至る
- 三連 backtick (` ``` ` fenced code block) も同様に危険
- 代替: コード片は引用符・インデント・タグ表記で代替するか、commit メッセージから除外する

### 外部スクリプト実行時のパイプの扱い

ファイル書き出しやログ生成を伴う外部スクリプトを実行する際、その出力に**バッファリング/切り詰めパイプを直接挟まない**。理由:

- 出力が見えなくなり、事後にエラー詳細やログを参照できなくなる
- `head` 等は SIGPIPE で上流プロセスを早期終了させる場合がある

**禁止例**:
- `script | head -N`
- `script | tail -N`
- `script > /dev/null`

**許容パターン**: `tee` で全出力をファイルに残してから表示を絞る。

- `script | tee /tmp/out.log | tail -n 50` — 全出力を `/tmp/out.log` に保存しつつ表示だけ tail に絞る
- 後で `cat /tmp/out.log` や `grep` で全文参照可能
- 標準エラーも残したい場合は `script 2>&1 | tee /tmp/out.log | tail -n 50`

## git 運用

### カレントディレクトリで実行する

gitコマンドは常にカレントディレクトリで実行する。`-C` オプションや絶対パス指定は使わない。

### force push の AI 代行実行

squashやamendして履歴をきれいにした場合、`git push --force-with-lease` の実行が必要であればユーザーに確認せずに実行する。
ただし、`--force` を使う場合・main 系ブランチへの push・squash 由来でない force push）はユーザーに確認を行う

### 外部公開コンテンツでのローカルパス取扱い

GitHub Issue / PR の本文・コメント、コミットメッセージ、公開リポジトリの Markdown、pastebin / gist 等の**外部公開コンテンツにはホーム配下絶対パス・認証情報・内部 host 名を直書きしない**。ユーザー名や環境情報が露呈する。

**禁止例**:
- `/Users/<name>/...`、`/home/<name>/...`、`C:\Users\<name>\...` 等のホーム配下絶対パス
- API トークン / 認証情報
- 内部 host 名 / 社内 URL

**推奨置換**:
- ホーム配下 → `~/...`
- リポジトリ配下 → repo-relative path（backtick 囲み、例: `` `bin/foo.sh` ``）
- 環境固有 prefix → プレースホルダ（例: `<homebrew-prefix>/bin/codex`）

**例外**: ローカル shell ログ / `/tmp/...` 中間ファイル等、外部公開しない作業ログは生パスのまま可。

**起票前チェック**: `gh issue create --body-file <f>` / `gh pr create --body-file <f>` 実行前に `grep -nE '/Users/|/home/[^/]+/|C:\\Users\\' <f>` で目視確認。

## エージェント連携

### Codex CLI との連携

出力は全てCodexにて検証される。
Codex にコードレビューを依頼する場合は以下のコマンドを使う：

```bash
# なんらかの依頼
codex exec "指示テキスト"

# コードレビュー（ベースブランチとの差分をレビュー）
codex review --base main

# セッションを再開して追加指示
codex exec resume <session-id> "追加の指示テキスト"
```

- `codex review` の出力にセッション ID が含まれる
- `codex exec resume` で同じセッションのコンテキストを維持したまま追加指示を送れる
- Bash ツールから実行可能（非対話モードで動作する）

### サブエージェント委譲を原則とする

main context を節約するため、出力が大きくなりがちなタスクは原則 Agent tool に委譲する:

- 未知コードベースの探索・3 クエリ以上の grep/find が必要な調査
- テスト実行・ビルド・lint・ログ解析など出力ボリュームが大きい実行系
- 結果がサマリで十分な自己完結型の作業

**直接実行してよい**:
- 既知ファイルの Read / 既知シンボルの 1〜2 発 grep
- 編集・コミット等の書き込み系（出力が短い）
- 結果の生データを後続 step で必ず使うケース

**判断基準**: 出力に `tail -N` や `head -N` で切り詰めたくなったら委譲する（「外部スクリプト実行時のパイプの扱い」も参照）。

**使い分け**:
- ファイル/シンボル探索 → `Explore` (Haiku, 高速)
- 設計プラン立案前のリサーチ → `Plan`
- テスト/ビルド実行・探索 + 変更を伴うマルチステップ → `general-purpose`
- 独立タスクは 1 message に複数 Agent tool use を含めて並列実行する

## 設定管理

### 設定ファイルのスコープ

- User-scoped: `~/.claude/settings.json`
- Project-scoped: `.claude/settings.json`
- Local-scoped: `.claude/settings.local.json`
- スコープが不明な場合は書き込む前にユーザーに確認すること

### セッションタイトル自動設定

- hooks で自動設定済み（SessionStart hook でシェルコマンド直接実行）
- フォーマット: `リポジトリ名 / ブランチ名`
- 必要に応じて手動でタイトルを変更可能
