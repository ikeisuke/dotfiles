.*
*.zwc
*.zwc.old
.DS_Store

# Track project-scoped Claude Code settings (but keep local overrides ignored)
!/.claude/
/.claude/settings.local.json
!/.mcp.json

# Secrets & credentials (safety net)
.env*
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
.aws/
.ssh/
credentials*
